name: CI

permissions: {}

on:
  pull_request:
  push:
    branches:
      - alpha
      - beta
      - master
      - renovate/**

jobs:
  ci-optimization:
    name: CI optimization
    uses: dargmuesli/github-actions/.github/workflows/ci-optimization.yml@2fb9bdae5c8ce52ea7f9546d1ba22c34921246de # 5.1.3
    permissions:
      pull-requests: read
  build:
    needs: ci-optimization
    if: needs.ci-optimization.outputs.continue == 'true'
    name: Validate `README.md`
    permissions:
      contents: read
    uses: dargmuesli/github-actions/.github/workflows/dargstack-rgen.yml@2fb9bdae5c8ce52ea7f9546d1ba22c34921246de # 5.1.3
  release-semantic:
    needs: build
    name: Semantic Release
    uses: dargmuesli/github-actions/.github/workflows/release-semantic.yml@2fb9bdae5c8ce52ea7f9546d1ba22c34921246de # 5.1.3
    permissions:
      contents: write
      id-token: write
    secrets:
      PERSONAL_ACCESS_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
